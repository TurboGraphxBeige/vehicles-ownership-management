export interface Service {
    service_id: string;
    vehicle_id: string;
    contact_id: string;
    notes: string;
    service_date: string;
    service_request_description: string;
    total_cost: number;
    invoice: string;
}